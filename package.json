{
  "name": "byb-engine",
  "version": "0.1.0",
  "private": true,
  "description": "BYB (Build Your Business) - Wizard-based ecommerce engine with cloud-agnostic adapter architecture",
  "workspaces": [
    "packages/*"
  ],
  "scripts": {
    "test": "vitest run",
    "test:watch": "vitest",
    "typecheck": "tsc --noEmit",
    "clean": "rm -rf packages/*/dist"
  },
  "devDependencies": {
    "typescript": "^5.7.0",
    "vitest": "^3.0.0"
  },
  "engines": {
    "node": ">=20"
  }
}
